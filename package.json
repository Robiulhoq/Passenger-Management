{
  "name": "passenger-management-system",
  "version": "1.0.0",
  "description": "MERN Stack Passenger Management System with CRUD operations",
  "scripts": {
  "dev": "concurrently \"npm run dev-server\" \"npm run dev-client\"",

  "dev-server": "cd server && npm run dev",
  "dev-client": "cd client && npm start",

  "build": "npm install && cd server && npm install && cd ../client && npm install && npm run build",

  "start": "cd server && npm start"
}
,
  "keywords": [
    "mern",
    "crud",
    "passenger",
    "management",
    "mongodb",
    "express",
    "react",
    "nodejs"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
